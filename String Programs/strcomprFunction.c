//program for making the string compare function manually 
#include<stdio.h>
#include<string.h>
int main(){
    char str1[50];
    char str2[50];
    //we are inputing only one word therefore taking input via scanf function
    printf("Enter the string 1\n");
    scanf("%s",str1);
    printf("Enter the string 2\n");
    scanf("%s",str2);
    return 0;
    if(strlen(str1) == strlen(str2)){
        
    }
}